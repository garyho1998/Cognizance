<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Account" />
        <activity
            android:name=".PopupLoginAlreadyExistsRegister"
            android:theme="@style/AppTheme.PopMe" />
        <activity
            android:name=".PopupEmailAlreadyExistsRegister"
            android:theme="@style/AppTheme.PopMe" />
        <activity
            android:name=".areYouSureToLogOut"
            android:theme="@style/AppTheme.PopMe" />
        <activity
            android:name=".PopUpNoLoginInfoProvided"
            android:theme="@style/AppTheme.PopMe" />
        <activity
            android:name=".PopupIfLoginOrPasswordIsInvalid"
            android:theme="@style/AppTheme.PopMe" />
        <activity
            android:name=".Register"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".timerpage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".exercise"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".main_menu"
            android:label="@string/app_name" />
        <activity android:name=".lottie_ninja">
        </activity>
        <activity android:name=".SpalshActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>